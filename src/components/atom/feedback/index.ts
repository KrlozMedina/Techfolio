/**
 * Archivo de barril (barrel file) para exportar todos los componentes
 * relacionados con feedback desde el directorio actual.
 *
 * Facilita la importación centralizada y mejora la organización del código,
 * permitiendo importar múltiples componentes desde un único punto.
 * 
 * Actualmente exporta:
 * - Spinner: Componente de carga (loading spinner).
 */

export * from './Spinner';
